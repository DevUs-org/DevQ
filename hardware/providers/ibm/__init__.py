'''
Tags: Provider

ibm provider — IBM simulated backend.

IBMSimulatedProvider wraps Qiskit V2 fake backends carrying real IBM
calibration data (Target API; native 2Q gate auto-discovered per
backend) with AerSimulator noise-model execution. IBMRealProvider
(live hardware via QiskitRuntimeService) is planned in Phase 7.
'''