'''
Tags: Main

hardware — Device abstraction layer.

QuantumDevice (pure data container), load_device() validation,
TopologyGraph, and the providers subpackage. The kernel only ever
sees QuantumDevice; provider-specific logic is fully encapsulated.
'''