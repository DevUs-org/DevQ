'''
Tags: Plugin

frontends — opt-in source-language frontends. Each subpackage reads one
source language (a Qiskit .py, OpenQASM 3) and lowers it to a CircuitRep
through the plugin seam. None is shipped in the built-in set; each is
registered by hand.
'''
