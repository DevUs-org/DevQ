'''
Tags: Main

kernel — DevQ's core execution engine.

The Kernel drives submission, scheduling, dispatch, and future
resolution; subpackages hold the process layer (QCB, ProcessTable,
lifecycle), memory layer (QubitPool, MemoryManager, allocators),
and scheduler layer.
'''