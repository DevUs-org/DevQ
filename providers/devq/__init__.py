'''
Tags: Provider

devq provider — DevQ's own simulated backend.

Pure-Python backend factory (four topologies, generated error maps)
and DevQSimulatedProvider with mocked execution. No external
dependencies; doubles as the reference implementation for provider
authors.
'''